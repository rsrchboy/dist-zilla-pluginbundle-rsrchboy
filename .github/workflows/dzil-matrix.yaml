name: "dzil matrix"
on:
  workflow_dispatch: ~
  push:
    branches:
      - "**"
      - "!release/cpan"
    tags-ignore: "*"
  pull_request: ~

jobs:
  build-and-test:
    name: dzil-matrix
    uses: rjbs/dzil-actions/.github/workflows/dzil-matrix.yaml@v0
